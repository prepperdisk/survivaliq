// SurvivalIQ runtime configuration
// Flip flags here to toggle developer/reviewer features.
const APP_CONFIG = {
  // Show a subtle "#id" label above each quiz question — useful for content review.
  showQuestionId: true
};
